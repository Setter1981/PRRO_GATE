=== STEP: 162 ===
TOOL: replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\src\\db\\repositories\\ingress_inbox.rs"
ARGS: {
  "AllowMultiple": "false",
  "Description": "\"Use RequestId and InboxStatus in IngressInboxRepo for type-safe wrappers\"",
  "EndLine": "288",
  "Instruction": "\"Update IngressInboxRepo to use RequestId and InboxStatus instead of raw [u8; 16] and String.\"",
  "ReplacementContent": "\"//! Repository for `ingress_inbox`.\\n//!\\n//! Implements spec §6.2 / decision #31 — the most subtle correctness gate\\n//! in M1: idempotent ingress with three distinct outcomes.\\n//!\\n//! Behaviour of `insert(NewInboxEntry)`:\\n//! - **Created** — first time `(fiscal_number, idempotency_key)` is seen;\\n//!   the row is persisted with `status='NEW'` and the populated row is\\n//!   returned.\\n//! - **Replay** — the same `(fn, idem_key)` is already on file AND the\\n//!   submitted `payload_sha256_canonical` matches the stored one; the\\n//!   existing row is returned.  Caller MUST treat this as success\\n//!   (idempotent retry of the same logical request) and MUST NOT\\n//!   re-process.\\n//! - **Conflict** — same `(fn, idem_key)` but a different payload hash;\\n//!   returns both hashes so the caller can audit the discrepancy.\\n//!   This is NEVER a silent replay — the gateway refuses to reuse an\\n//!   idempotency key for a different payload.\\n//!\\n//! All branches run inside a single `db::tx::with_immediate` transaction\\n//! so the probe-then-insert is atomic against concurrent ingress\\n//! attempts on the same key.  This is the canonical use-site for the\\n//! `with_immediate` primitive in M1.\\n\\nuse crate::db::models::enums::{InboxStatus, Protocol};\\nuse crate::db::models::ids::RequestId;\\nuse crate::db::tx::{with_immediate, WriteTxConn};\\nuse sqlx::SqlitePool;\\n\\n#[derive(Debug, Clone)]\\npub struct InboxRow {\\n    pub request_id: RequestId,\\n    pub fiscal_number: String,\\n    pub protocol: Protocol,\\n    pub operation_type: String,\\n    pub idempotency_key: String,\\n    pub status: InboxStatus,\\n    pub payload_json: String,\\n    pub payload_sha256_canonical: [u8; 32],\\n    pub correlation_id: Option<String>,\\n    pub received_at: String,\\n}\\n\\n#[derive(Debug, Clone)]\\npub struct NewInboxEntry {\\n    pub request_id: RequestId,\\n    pub fiscal_number: String,\\n    pub protocol: Protocol,\\n    pub operation_type: String,\\n    pub idempotency_key: String,\\n    pub payload_json: String,\\n    pub payload_sh\n<truncated 9567 bytes>",
  "StartLine": "1",
  "TargetContent": "\"//! Repository for `ingress_inbox`.\\n//!\\n//! Implements spec §6.2 / decision #31 — the most subtle correctness gate\\n//! in M1: idempotent ingress with three distinct outcomes.\\n//!\\n//! Behaviour of `insert(NewInboxEntry)`:\\n//! - **Created** — first time `(fiscal_number, idempotency_key)` is seen;\\n//!   the row is persisted with `status='NEW'` and the populated row is\\n//!   returned.\\n//! - **Replay** — the same `(fn, idem_key)` is already on file AND the\\n//!   submitted `payload_sha256_canonical` matches the stored one; the\\n//!   existing row is returned.  Caller MUST treat this as success\\n//!   (idempotent retry of the same logical request) and MUST NOT\\n//!   re-process.\\n//! - **Conflict** — same `(fn, idem_key)` but a different payload hash;\\n//!   returns both hashes so the caller can audit the discrepancy.\\n//!   This is NEVER a silent replay — the gateway refuses to reuse an\\n//!   idempotency key for a different payload.\\n//!\\n//! All branches run inside a single `db::tx::with_immediate` transaction\\n//! so the probe-then-insert is atomic against concurrent ingress\\n//! attempts on the same key.  This is the canonical use-site for the\\n//! `with_immediate` primitive in M1.\\n\\nuse crate::db::models::enums::Protocol;\\nuse crate::db::tx::{with_immediate, WriteTxConn};\\nuse sqlx::SqlitePool;\\n\\n#[derive(Debug, Clone)]\\npub struct InboxRow {\\n    pub request_id: [u8; 16],\\n    pub fiscal_number: String,\\n    pub protocol: Protocol,\\n    pub operation_type: String,\\n    pub idempotency_key: String,\\n    pub status: String,\\n    pub payload_json: String,\\n    pub payload_sha256_canonical: [u8; 32],\\n    pub correlation_id: Option<String>,\\n    pub received_at: String,\\n}\\n\\n#[derive(Debug, Clone)]\\npub struct NewInboxEntry {\\n    pub request_id: [u8; 16],\\n    pub fiscal_number: String,\\n    pub protocol: Protocol,\\n    pub operation_type: String,\\n    pub idempotency_key: String,\\n    pub payload_json: String,\\n    pub payload_sha256_canonical: [u8; 32],\\n    pub correlation_id: Option<Stri\n<truncated 9952 bytes>",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\src\\\\db\\\\repositories\\\\ingress_inbox.rs\"",
  "toolAction": "\"Updating ingress_inbox.rs with typed wrappers\"",
  "toolSummary": "\"Edit ingress_inbox.rs\""
}
========================================

=== STEP: 165 ===
TOOL: replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\src\\services\\write_path\\stage_acquire.rs"
ARGS: {
  "AllowMultiple": "false",
  "Description": "\"Update stage_acquire.rs to use RequestId instead of raw [u8; 16]\"",
  "EndLine": "327",
  "Instruction": "\"Change [u8; 16] to RequestId in stage_acquire::run and reject signatures and calls.\"",
  "ReplacementContent": "\"pub async fn run(\\n    pool: &SqlitePool,\\n    request_id: RequestId,\\n    command: CanonicalFiscalCommand,\\n) -> anyhow::Result<WorkerProcessResult> {\\n    with_immediate(pool, move |tx| {\\n        Box::pin(async move {\\n            // [Step 1] Acquire lease — CAS NEW → PROCESSING.\\n            let inbox = match ingress_inbox::acquire_lease(tx, &request_id).await? {\\n                Some(row) => row,\\n                None => return Ok(WorkerProcessResult::Noop),\\n            };\\n            let fn_id = inbox.fiscal_number.clone();\\n\\n            // [Step 1b] Command-vs-inbox cross-check.  The leased\\n            //           inbox row carries `payload_json`,\\n            //           `payload_sha256_canonical`, and\\n            //           `operation_type` persisted by ingress; the\\n            //           in-process `command` argument MUST agree on\\n            //           hash and doc_type, otherwise the worker is\\n            //           about to PREPARE a doc against payload it did\\n            //           not receive.  Reject without lnd advance and\\n            //           without INSERT.\\n            if command.payload_sha256_canonical != inbox.payload_sha256_canonical {\\n                return reject(\\n                    tx,\\n                    &request_id,\\n                    RejectionReason::InvalidPayload {\\n                        detail: \\\"command_payload_hash_mismatch\\\".to_string(),\\n                    },\\n                    \\\"command_inbox_mismatch\\\",\\n                    Severity::Critical,\\n                )\\n                .await;\\n            }\\n            if command.doc_type.as_str() != inbox.operation_type {\\n                return reject(\\n                    tx,\\n                    &request_id,\\n                    RejectionReason::InvalidPayload {\\n                        detail: \\\"command_doc_type_mismatch\\\".to_string(),\\n                    },\\n                    \\\"command_inbox_mismatch\\\",\\n                    Severity::Critical,\\n                )\\n                \n<truncated 9771 bytes>",
  "StartLine": "44",
  "TargetContent": "\"pub async fn run(\\n    pool: &SqlitePool,\\n    request_id: [u8; 16],\\n    command: CanonicalFiscalCommand,\\n) -> anyhow::Result<WorkerProcessResult> {\\n    with_immediate(pool, move |tx| {\\n        Box::pin(async move {\\n            // [Step 1] Acquire lease — CAS NEW → PROCESSING.\\n            let inbox = match ingress_inbox::acquire_lease(tx, &request_id).await? {\\n                Some(row) => row,\\n                None => return Ok(WorkerProcessResult::Noop),\\n            };\\n            let fn_id = inbox.fiscal_number.clone();\\n\\n            // [Step 1b] Command-vs-inbox cross-check.  The leased\\n            //           inbox row carries `payload_json`,\\n            //           `payload_sha256_canonical`, and\\n            //           `operation_type` persisted by ingress; the\\n            //           in-process `command` argument MUST agree on\\n            //           hash and doc_type, otherwise the worker is\\n            //           about to PREPARE a doc against payload it did\\n            //           not receive.  Reject without lnd advance and\\n            //           without INSERT.\\n            if command.payload_sha256_canonical != inbox.payload_sha256_canonical {\\n                return reject(\\n                    tx,\\n                    &request_id,\\n                    RejectionReason::InvalidPayload {\\n                        detail: \\\"command_payload_hash_mismatch\\\".to_string(),\\n                    },\\n                    \\\"command_inbox_mismatch\\\",\\n                    Severity::Critical,\\n                )\\n                .await;\\n            }\\n            if command.doc_type.as_str() != inbox.operation_type {\\n                return reject(\\n                    tx,\\n                    &request_id,\\n                    RejectionReason::InvalidPayload {\\n                        detail: \\\"command_doc_type_mismatch\\\".to_string(),\\n                    },\\n                    \\\"command_inbox_mismatch\\\",\\n                    Severity::Critical,\\n                )\\n                .\n<truncated 9761 bytes>",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\src\\\\services\\\\write_path\\\\stage_acquire.rs\"",
  "toolAction": "\"Updating stage_acquire.rs to use RequestId\"",
  "toolSummary": "\"Edit stage_acquire.rs\""
}
========================================

=== STEP: 175 ===
TOOL: replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\src\\db\\repositories\\fiscal_documents.rs"
ARGS: {
  "AllowMultiple": "false",
  "Description": "\"Update FinalizeInputs and fetch_finalize_inputs_tx to use RequestId\"",
  "EndLine": "754",
  "Instruction": "\"Change FinalizeInputs request_id field and fetch_finalize_inputs_tx return construction to use RequestId directly.\"",
  "ReplacementContent": "\"/// row DONE).\\n#[derive(Debug, Clone)]\\npub struct FinalizeInputs {\\n    pub fiscal_number: String,\\n    /// W8 review F1 close: read from the doc row, NOT from caller.\\n    /// Used to drive `ingress_inbox::mark_done_tx` so a wrong\\n    /// caller-supplied request_id can never advance an unrelated\\n    /// inbox row.\\n    pub request_id: RequestId,\\n    pub lnd: i64,\\n    /// `Some` after W6 stage 3-PERSIST writes it; `None` only on a\\n    /// freshly-PREPARED row.  Post-Kvt2 invariant: `Some` MUST hold;\\n    /// stage_finalize::run surfaces `None` as\\n    /// `StageFinalizeError::UnsignedXmlShaMissing`.\\n    pub unsigned_xml_sha256: Option<[u8; 32]>,\\n    /// `Some` for non-genesis docs (set in W6 stage 3-PRE pin); `None`\\n    /// for the very first doc-after-bootstrap.  Used (W8 review F2\\n    /// close) for the chain-continuity guard: must equal the current\\n    /// `node_state.last_known_unsigned_xml_sha256` (None == None for\\n    /// genesis), else `ChainSeedMismatch` typed error + rollback.\\n    /// Also surfaced into the `STAGE_FINALIZE_ACK` audit payload.\\n    pub previous_hash: Option<[u8; 32]>,\\n    /// Schema-NOT-NULL on `fiscal_documents` (set at W5 stage 1\\n    /// INSERT PREPARED).  Copied into `outbox.payload_sha256` so the\\n    /// post-M3a publisher worker can cross-correlate the queue row\\n    /// with the canonical payload archive.\\n    pub payload_sha256_canonical: [u8; 32],\\n}\\n\\n/// W8 stage 5 finalize — read the minimal field set required for\\n/// post-CAS bookkeeping in a single SELECT inside the same\\n/// `with_immediate` envelope as the CAS `Kvt2 → Ack`.  Returns\\n/// `None` when the row vanished between the CAS Applied and this\\n/// read (impossible under M3a's single-writer-per-FN invariant —\\n/// see ADR-M3-A10 — + the CAS we just committed, but typed\\n/// defensively rather than panicking).\\npub async fn fetch_finalize_inputs_tx(\\n    tx: &mut WriteTxConn<'_>,\\n    doc_id: DocumentId,\\n) -> sqlx::Result<Option<FinalizeInputs>> {\\n    let row = sqlx::query!(\\n        r#\\\"SE\n<truncated 1368 bytes>",
  "StartLine": "671",
  "TargetContent": "\"#[derive(Debug, Clone)]\\npub struct FinalizeInputs {\\n    pub fiscal_number: String,\\n    /// W8 review F1 close: read from the doc row, NOT from caller.\\n    /// Used to drive `ingress_inbox::mark_done_tx` so a wrong\\n    /// caller-supplied request_id can never advance an unrelated\\n    /// inbox row.\\n    pub request_id: [u8; 16],\\n    pub lnd: i64,\\n    /// `Some` after W6 stage 3-PERSIST writes it; `None` only on a\\n    /// freshly-PREPARED row.  Post-Kvt2 invariant: `Some` MUST hold;\\n    /// stage_finalize::run surfaces `None` as\\n    /// `StageFinalizeError::UnsignedXmlShaMissing`.\\n    pub unsigned_xml_sha256: Option<[u8; 32]>,\\n    /// `Some` for non-genesis docs (set in W6 stage 3-PRE pin); `None`\\n    /// for the very first doc-after-bootstrap.  Used (W8 review F2\\n    /// close) for the chain-continuity guard: must equal the current\\n    /// `node_state.last_known_unsigned_xml_sha256` (None == None for\\n    /// genesis), else `ChainSeedMismatch` typed error + rollback.\\n    /// Also surfaced into the `STAGE_FINALIZE_ACK` audit payload.\\n    pub previous_hash: Option<[u8; 32]>,\\n    /// Schema-NOT-NULL on `fiscal_documents` (set at W5 stage 1\\n    /// INSERT PREPARED).  Copied into `outbox.payload_sha256` so the\\n    /// post-M3a publisher worker can cross-correlate the queue row\\n    /// with the canonical payload archive.\\n    pub payload_sha256_canonical: [u8; 32],\\n}\\n\\n/// W8 stage 5 finalize — read the minimal field set required for\\n/// post-CAS bookkeeping in a single SELECT inside the same\\n/// `with_immediate` envelope as the CAS `Kvt2 → Ack`.  Returns\\n/// `None` when the row vanished between the CAS Applied and this\\n/// read (impossible under M3a's single-writer-per-FN invariant —\\n/// see ADR-M3-A10 — + the CAS we just committed, but typed\\n/// defensively rather than panicking).\\npub async fn fetch_finalize_inputs_tx(\\n    tx: &mut WriteTxConn<'_>,\\n    doc_id: DocumentId,\\n) -> sqlx::Result<Option<FinalizeInputs>> {\\n    let row = sqlx::query!(\\n        r#\\\"SELECT fiscal_numbe\n<truncated 1639 bytes>",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\src\\\\db\\\\repositories\\\\fiscal_documents.rs\"",
  "toolAction": "\"Updating FinalizeInputs and fetch_finalize_inputs_tx\"",
  "toolSummary": "\"Edit fiscal_documents.rs\""
}
========================================

=== STEP: 177 ===
TOOL: replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\src\\services\\write_path\\stage_finalize.rs"
ARGS: {
  "AllowMultiple": "false",
  "Description": "\"Update stage_finalize.rs to use RequestId for InboxDoneMissing\"",
  "EndLine": "110",
  "Instruction": "\"Import RequestId and update StageFinalizeError::InboxDoneMissing request_id field to use RequestId instead of [u8; 16].\"",
  "ReplacementContent": "\"use crate::db::models::enums::{DocState, Severity};\\nuse crate::db::models::ids::{DocumentId, RequestId};\\nuse crate::db::repositories::fiscal_documents::TransitionOutcome;\\nuse crate::db::repositories::outbox::NewOutboxRow;\\nuse crate::db::repositories::{\\n    audit_log, fiscal_documents as fd, ingress_inbox, node_state, outbox,\\n};\\nuse crate::db::tx::with_immediate;\\n\\n// ─── Errors ──────────────────────────────────────────────────────────\\n\\n/// Stage-5 typed error surface.  All variants describe state-invariant\\n/// breaches or DB failures; idempotent re-entry and document-missing\\n/// paths are NOT errors (see [`StageFinalizeOutcome`]).\\n#[non_exhaustive]\\n#[derive(Debug, thiserror::Error)]\\npub enum StageFinalizeError {\\n    /// Doc reached `Kvt2` but `unsigned_xml_sha256 IS NULL` —\\n    /// state-invariant breach (W6 stage 3-PERSIST writes the column\\n    /// before any state can advance past `Signed`).  Without the\\n    /// hash there is no MAC chain seed to advance, so finalize\\n    /// fail-closes; the tx rolls back entirely (no Ack persisted).\\n    #[error(\\\"stage 5 unsigned_xml_sha256 missing for doc {document_id:?} despite reaching Kvt2\\\")]\\n    UnsignedXmlShaMissing { document_id: DocumentId },\\n\\n    /// Doc's `previous_hash` does not match the FN's current\\n    /// `node_state.last_known_unsigned_xml_sha256` (the chain seed\\n    /// from the previously-Acked doc).  Out-of-order finalize\\n    /// (e.g. W9 boot recovery iterating Kvt2 docs in wrong lnd\\n    /// order) would silently corrupt the next-doc MAC chain seed\\n    /// without this guard.\\n    ///\\n    /// Per W8 review F2: chain-continuity is enforced at finalize,\\n    /// not just by the W9 design constraint that recovery must sort\\n    /// by lnd.  `expected` is the doc's `previous_hash` (what the\\n    /// chain SHOULD point to upon entering finalize), `actual` is\\n    /// the FN's `last_known_unsigned_xml_sha256`\n<truncated 1310 bytes>",
  "StartLine": "43",
  "TargetContent": "\"use crate::db::models::enums::{DocState, Severity};\\nuse crate::db::models::ids::DocumentId;\\nuse crate::db::repositories::fiscal_documents::TransitionOutcome;\\nuse crate::db::repositories::outbox::NewOutboxRow;\\nuse crate::db::repositories::{\\n    audit_log, fiscal_documents as fd, ingress_inbox, node_state, outbox,\\n};\\nuse crate::db::tx::with_immediate;\\n\\n// ─── Errors ──────────────────────────────────────────────────────────\\n\\n/// Stage-5 typed error surface.  All variants describe state-invariant\\n/// breaches or DB failures; idempotent re-entry and document-missing\\n/// paths are NOT errors (see [`StageFinalizeOutcome`]).\\n#[non_exhaustive]\\n#[derive(Debug, thiserror::Error)]\\npub enum StageFinalizeError {\\n    /// Doc reached `Kvt2` but `unsigned_xml_sha256 IS NULL` —\\n    /// state-invariant breach (W6 stage 3-PERSIST writes the column\\n    /// before any state can advance past `Signed`).  Without the\\n    /// hash there is no MAC chain seed to advance, so finalize\\n    /// fail-closes; the tx rolls back entirely (no Ack persisted).\\n    #[error(\\\"stage 5 unsigned_xml_sha256 missing for doc {document_id:?} despite reaching Kvt2\\\")]\\n    UnsignedXmlShaMissing { document_id: DocumentId },\\n\\n    /// Doc's `previous_hash` does not match the FN's current\\n    /// `node_state.last_known_unsigned_xml_sha256` (the chain seed\\n    /// from the previously-Acked doc).  Out-of-order finalize\\n    /// (e.g. W9 boot recovery iterating Kvt2 docs in wrong lnd\\n    /// order) would silently corrupt the next-doc MAC chain seed\\n    /// without this guard.\\n    ///\\n    /// Per W8 review F2: chain-continuity is enforced at finalize,\\n    /// not just by the W9 design constraint that recovery must sort\\n    /// by lnd.  `expected` is the doc's `previous_hash` (what the\\n    /// chain SHOULD point to upon entering finalize), `actual` is\\n    /// the FN's `last_known_unsigned_xml_sha256` (what the ch\n<truncated 1296 bytes>",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\src\\\\services\\\\write_path\\\\stage_finalize.rs\"",
  "toolAction": "\"Updating stage_finalize.rs imports and InboxDoneMissing error\"",
  "toolSummary": "\"Edit stage_finalize.rs\""
}
========================================

=== STEP: 185 ===
TOOL: replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\src\\services\\reconciliation\\boot_phase.rs"
ARGS: {
  "AllowMultiple": "false",
  "Description": "\"Import RequestId and InboxStatus in boot_phase.rs\"",
  "EndLine": "45",
  "Instruction": "\"Add RequestId and InboxStatus to imports in boot_phase.rs.\"",
  "ReplacementContent": "\"use crate::db::models::enums::{DocState, InboxStatus, NodeMode, Protocol, Severity, ShiftState};\\nuse crate::db::models::ids::{DocumentId, RequestId, ShiftId};\"",
  "StartLine": "38",
  "TargetContent": "\"use crate::db::models::enums::{DocState, NodeMode, Protocol, Severity, ShiftState};\\nuse crate::db::models::ids::{DocumentId, ShiftId};\"",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\src\\\\services\\\\reconciliation\\\\boot_phase.rs\"",
  "toolAction": "\"Importing RequestId and InboxStatus in boot_phase.rs\"",
  "toolSummary": "\"Edit boot_phase.rs\""
}
========================================

=== STEP: 187 ===
TOOL: replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\src\\services\\reconciliation\\boot_phase.rs"
ARGS: {
  "AllowMultiple": "false",
  "Description": "\"Use RequestId and InboxStatus in prepared recovery snapshot inside boot_phase.rs\"",
  "EndLine": "2045",
  "Instruction": "\"Update prepared recovery snapshot to decode request_id as RequestId and status as InboxStatus directly.\"",
  "ReplacementContent": "\"            let inbox_request_id: RequestId = inbox_row_db.try_get(\\\"request_id\\\")?;\\n            let inbox_sha_v: Vec<u8> = inbox_row_db.try_get(\\\"payload_sha256_canonical\\\")?;\\n            let inbox_sha: [u8; 32] = inbox_sha_v.as_slice().try_into().map_err(|_| {\\n                anyhow::anyhow!(\\n                    \\\"ingress_inbox.payload_sha256_canonical length != 32 for doc {doc_id:?}\\\"\\n                )\\n            })?;\\n            let inbox_row = InboxRow {\\n                request_id: inbox_request_id,\\n                fiscal_number: inbox_row_db.try_get(\\\"fiscal_number\\\")?,\\n                protocol: inbox_row_db.try_get::<Protocol, _>(\\\"protocol\\\")?,\\n                operation_type: inbox_row_db.try_get(\\\"operation_type\\\")?,\\n                idempotency_key: inbox_row_db.try_get(\\\"idempotency_key\\\")?,\\n                status: inbox_row_db.try_get::<InboxStatus, _>(\\\"status\\\")?,\\n                payload_json: inbox_row_db.try_get(\\\"payload_json\\\")?,\\n                payload_sha256_canonical: inbox_sha,\\n                correlation_id: inbox_row_db.try_get(\\\"correlation_id\\\")?,\\n                received_at: inbox_row_db.try_get(\\\"received_at\\\")?,\\n            };\"",
  "StartLine": "2018",
  "TargetContent": "\"            let inbox_request_id_v: Vec<u8> = inbox_row_db.try_get(\\\"request_id\\\")?;\\n            let inbox_request_id: [u8; 16] =\\n                inbox_request_id_v.as_slice().try_into().map_err(|_| {\\n                    anyhow::anyhow!(\\\"ingress_inbox.request_id length != 16 for doc {doc_id:?}\\\")\\n                })?;\\n            let inbox_sha_v: Vec<u8> = inbox_row_db.try_get(\\\"payload_sha256_canonical\\\")?;\\n            let inbox_sha: [u8; 32] = inbox_sha_v.as_slice().try_into().map_err(|_| {\\n                anyhow::anyhow!(\\n                    \\\"ingress_inbox.payload_sha256_canonical length != 32 for doc {doc_id:?}\\\"\\n                )\\n            })?;\\n            let inbox_row = InboxRow {\\n                request_id: inbox_request_id,\\n                fiscal_number: inbox_row_db.try_get(\\\"fiscal_number\\\")?,\\n                protocol: inbox_row_db.try_get::<Protocol, _>(\\\"protocol\\\")?,\\n                operation_type: inbox_row_db.try_get(\\\"operation_type\\\")?,\\n                idempotency_key: inbox_row_db.try_get(\\\"idempotency_key\\\")?,\\n                status: inbox_row_db.try_get(\\\"status\\\")?,\\n                payload_json: inbox_row_db.try_get(\\\"payload_json\\\")?,\\n                payload_sha256_canonical: inbox_sha,\\n                correlation_id: inbox_row_db.try_get(\\\"correlation_id\\\")?,\\n                received_at: inbox_row_db.try_get(\\\"received_at\\\")?,\\n            };\"",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\src\\\\services\\\\reconciliation\\\\boot_phase.rs\"",
  "toolAction": "\"Update prepared recovery snapshot in boot_phase.rs\"",
  "toolSummary": "\"Edit boot_phase.rs\""
}
========================================

=== STEP: 273 ===
TOOL: replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\tests\\write_path_stage1_acquire.rs"
ARGS: {
  "AllowMultiple": "false",
  "Description": "\"Refactor seed_inbox_new, seed_inbox_with_overrides, and inbox_status in write_path_stage1_acquire.rs to use type-safe RequestId.\"",
  "EndLine": "182",
  "Instruction": "\"Change the signature and body of seed_inbox_new, seed_inbox_with_overrides, and inbox_status to use RequestId instead of [u8; 16]\"",
  "ReplacementContent": "\"async fn seed_inbox_new(pool: &sqlx::SqlitePool, doc_type: DocType) -> RequestId {\\n    seed_inbox_with_overrides(pool, doc_type.as_str(), [0u8; 32]).await\\n}\\n\\n/// Same as `seed_inbox_new` but lets the caller provide a different\\n/// `operation_type` string and / or a different `payload_sha256_canonical`\\n/// to drive the command-vs-inbox mismatch fixtures.\\nasync fn seed_inbox_with_overrides(\\n    pool: &sqlx::SqlitePool,\\n    operation_type: &str,\\n    payload_sha256_canonical: [u8; 32],\\n) -> RequestId {\\n    let req_id = RequestId::new();\\n    let req_bytes: [u8; 16] = *req_id.as_bytes();\\n    inbox::insert(\\n        pool,\\n        &NewInboxEntry {\\n            request_id: req_id,\\n            fiscal_number: FN.into(),\\n            protocol: prro::db::models::enums::Protocol::Rest,\\n            operation_type: operation_type.into(),\\n            idempotency_key: format!(\\\"idem-{}\\\", hex::encode(req_bytes)),\\n            payload_json: r#\\\"{\\\"goods\\\":[]}\\\"#.into(),\\n            payload_sha256_canonical,\\n            correlation_id: None,\\n        },\\n    )\\n    .await\\n    .unwrap();\\n    req_id\\n}\\n\\nfn cmd(doc_type: DocType) -> CanonicalFiscalCommand {\\n    CanonicalFiscalCommand {\\n        doc_type,\\n        business_ts: \\\"2026-04-22T12:00:00Z\\\".into(),\\n        total_sum_kop: Some(15000),\\n        payload_json: r#\\\"{\\\"goods\\\":[]}\\\"#.into(),\\n        payload_sha256_canonical: [0u8; 32],\\n    }\\n}\\n\\nasync fn doc_count(pool: &sqlx::SqlitePool) -> i64 {\\n    sqlx::query_scalar(\\\"SELECT COUNT(*) FROM fiscal_documents\\\")\\n        .fetch_one(pool)\\n        .await\\n        .unwrap()\\n}\\n\\nasync fn next_lnd(pool: &sqlx::SqlitePool) -> i64 {\\n    sqlx::query_scalar(\\\"SELECT next_lnd FROM node_state WHERE fiscal_number = ?\\\")\\n        .bind(FN)\\n        .fetch_one(pool)\\n        .await\\n        .unwrap()\\n}\\n\\nasync fn inbox_status(pool: &sqlx::SqlitePool, req_id: &RequestId) -> String {\\n    sqlx::query_scalar(\\\"SELECT status FROM ingress_inbox WHERE request_id = ?\\\")\\n        .bind(req_id)\\n        .fetch_one(po\n<truncated 42 bytes>",
  "StartLine": "118",
  "TargetContent": "\"async fn seed_inbox_new(pool: &sqlx::SqlitePool, doc_type: DocType) -> [u8; 16] {\\n    seed_inbox_with_overrides(pool, doc_type.as_str(), [0u8; 32]).await\\n}\\n\\n/// Same as `seed_inbox_new` but lets the caller provide a different\\n/// `operation_type` string and / or a different `payload_sha256_canonical`\\n/// to drive the command-vs-inbox mismatch fixtures.\\nasync fn seed_inbox_with_overrides(\\n    pool: &sqlx::SqlitePool,\\n    operation_type: &str,\\n    payload_sha256_canonical: [u8; 32],\\n) -> [u8; 16] {\\n    let req_id = RequestId::new();\\n    let req_bytes: [u8; 16] = *req_id.as_bytes();\\n    inbox::insert(\\n        pool,\\n        &NewInboxEntry {\\n            request_id: req_bytes,\\n            fiscal_number: FN.into(),\\n            protocol: prro::db::models::enums::Protocol::Rest,\\n            operation_type: operation_type.into(),\\n            idempotency_key: format!(\\\"idem-{}\\\", hex::encode(req_bytes)),\\n            payload_json: r#\\\"{\\\"goods\\\":[]}\\\"#.into(),\\n            payload_sha256_canonical,\\n            correlation_id: None,\\n        },\\n    )\\n    .await\\n    .unwrap();\\n    req_bytes\\n}\\n\\nfn cmd(doc_type: DocType) -> CanonicalFiscalCommand {\\n    CanonicalFiscalCommand {\\n        doc_type,\\n        business_ts: \\\"2026-04-22T12:00:00Z\\\".into(),\\n        total_sum_kop: Some(15000),\\n        payload_json: r#\\\"{\\\"goods\\\":[]}\\\"#.into(),\\n        payload_sha256_canonical: [0u8; 32],\\n    }\\n}\\n\\nasync fn doc_count(pool: &sqlx::SqlitePool) -> i64 {\\n    sqlx::query_scalar(\\\"SELECT COUNT(*) FROM fiscal_documents\\\")\\n        .fetch_one(pool)\\n        .await\\n        .unwrap()\\n}\\n\\nasync fn next_lnd(pool: &sqlx::SqlitePool) -> i64 {\\n    sqlx::query_scalar(\\\"SELECT next_lnd FROM node_state WHERE fiscal_number = ?\\\")\\n        .bind(FN)\\n        .fetch_one(pool)\\n        .await\\n        .unwrap()\\n}\\n\\nasync fn inbox_status(pool: &sqlx::SqlitePool, req_id: &[u8; 16]) -> String {\\n    let req_slice: &[u8] = req_id;\\n    sqlx::query_scalar(\\\"SELECT status FROM ingress_inbox WHERE request_id = ?\\\")\\n     \n<truncated 84 bytes>",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\tests\\\\write_path_stage1_acquire.rs\"",
  "toolAction": "\"Update write_path_stage1_acquire.rs helpers to use RequestId\"",
  "toolSummary": "\"Edit test helpers\""
}
========================================

=== STEP: 306 ===
TOOL: multi_replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\tests\\finalize_helpers.rs"
ARGS: {
  "Description": "\"Refactor finalize_helpers.rs to use RequestId instead of [u8; 16]\"",
  "Instruction": "\"Update finalize_helpers.rs to use RequestId for inbox/doc seeding.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":32,\"ReplacementContent\":\"use prro::db::models::enums::{NodeMode, ShiftState};\\nuse prro::db::models::ids::RequestId;\\nuse prro::db::repositories::{ingress_inbox, node_state};\\nuse prro::db::tx::with_immediate;\\nuse sqlx::SqlitePool;\",\"StartLine\":29,\"TargetContent\":\"use prro::db::models::enums::{NodeMode, ShiftState};\\nuse prro::db::repositories::{ingress_inbox, node_state};\\nuse prro::db::tx::with_immediate;\\nuse sqlx::SqlitePool;\"},{\"AllowMultiple\":false,\"EndLine\":94,\"ReplacementContent\":\"async fn seed_inbox_row_processing(pool: &SqlitePool, req_byte: u8) -> RequestId {\\n    seed_fn_config(pool).await;\\n    let req_bytes = [req_byte; 16];\\n    let req_id = RequestId::from_bytes(req_bytes);\\n    let sha = vec![0u8; 32];\\n    sqlx::query(\\n        \\\"INSERT INTO ingress_inbox(request_id, fiscal_number, protocol, operation_type, \\\\\\n            idempotency_key, payload_json, payload_sha256_canonical, status) \\\\\\n         VALUES (?, '1234567890', 'REST', 'sell', ?, '{}', ?, 'PROCESSING')\\\",\\n    )\\n    .bind(&req_id)\\n    .bind(format!(\\\"idem-{req_byte:02x}\\\"))\\n    .bind(&sha)\\n    .execute(pool)\\n    .await\\n    .expect(\\\"seed inbox PROCESSING\\\");\\n    req_id\\n}\\n\\nasync fn read_node_seed(pool: &SqlitePool) -> Option<Vec<u8>> {\\n    sqlx::query_scalar(\\n        \\\"SELECT last_known_unsigned_xml_sha256 FROM node_state WHERE fiscal_number = '1234567890'\\\",\\n    )\\n    .fetch_one(pool)\\n    .await\\n    .expect(\\\"read seed\\\")\\n}\\n\\nasync fn read_inbox_status(pool: &SqlitePool, req_id: &RequestId) -> Option<String> {\\n    sqlx::query_scalar(\\\"SELECT status FROM ingress_inbox WHERE request_id = ?\\\")\\n        .bind(req_id)\\n        .fetch_one(pool)\\n        .await\\n        .ok()\\n}\",\"StartLine\":59,\"TargetContent\":\"async fn seed_inbox_row_processing(pool: &SqlitePool, req_byte: u8) -> [u8; 16] {\\n    seed_fn_config(pool).await;\\n    let req_id = [req_byte; 16];\\n    let req_slice: &[u8] = &req_id;\\n    let sha = vec![0u8; 32];\\n    sqlx::query(\\n        \\\"INSERT INTO ingress_inbox(request_i\n<truncated 2621 bytes>",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\tests\\\\finalize_helpers.rs\"",
  "toolAction": "\"Multi-replace finalizer helper tests to use RequestId\"",
  "toolSummary": "\"Edit test helpers\""
}
========================================

=== STEP: 316 ===
TOOL: multi_replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\tests\\write_path_stage3_sign.rs"
ARGS: {
  "Description": "\"Refactor seed_prepared_doc and make_worker_context in write_path_stage3_sign.rs to use RequestId instead of [u8; 16] and InboxStatus::Processing instead of String.\"",
  "Instruction": "\"Change seed_prepared_doc and make_worker_context to use RequestId and InboxStatus::Processing.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":264,\"ReplacementContent\":\"async fn seed_prepared_doc(\\n    pool: &sqlx::SqlitePool,\\n    doc_type: DocType,\\n    payload_json: &str,\\n    payload_sha256: [u8; 32],\\n    lnd: i64,\\n) -> (DocumentId, RequestId) {\\n    let doc_id = DocumentId::new();\\n    let req_id = RequestId::new();\\n    let req_bytes: [u8; 16] = *req_id.as_bytes();\\n    // Mirror fiscal_documents columns exactly.\\n    sqlx::query(\\n        \\\"INSERT INTO fiscal_documents (\\n             document_id, request_id, fiscal_number, lnd, doc_type, state,\\n             backend_profile_id, transport_profile_id, fs_mode, business_ts,\\n             total_sum_kop, payload_json, payload_sha256_canonical\\n         ) VALUES (?, ?, ?, ?, ?, 'PREPARED', 'b', 't', 'ONLINE', '2026-04-22T12:00:00Z', 15000, ?, ?)\\\",\\n    )\\n    .bind(doc_id)\\n    .bind(req_id)\\n    .bind(FN)\\n    .bind(lnd)\\n    .bind(doc_type)\\n    .bind(payload_json)\\n    .bind(&payload_sha256[..])\\n    .execute(pool)\\n    .await\\n    .unwrap();\\n    // Also seed a matching inbox row (W5-canonical operation_type).\\n    inbox::insert(\\n        pool,\\n        &NewInboxEntry {\\n            request_id: req_id,\\n            fiscal_number: FN.into(),\\n            protocol: prro::db::models::enums::Protocol::Rest,\\n            operation_type: doc_type.as_str().into(),\\n            idempotency_key: format!(\\\"idem-{}\\\", hex_encode(&req_bytes)),\\n            payload_json: payload_json.into(),\\n            payload_sha256_canonical: payload_sha256,\\n            correlation_id: None,\\n        },\\n    )\\n    .await\\n    .unwrap();\\n    (doc_id, req_id)\\n}\",\"StartLine\":219,\"TargetContent\":\"async fn seed_prepared_doc(\\n    pool: &sqlx::SqlitePool,\\n    doc_type: DocType,\\n    payload_json: &str,\\n    payload_sha256: [u8; 32],\\n    lnd: i64,\\n) -> (DocumentId, [u8; 16]) {\\n    let doc_id = DocumentId::new();\\n    let req_id = RequestId::new();\\n    let req_bytes: [u8; 16] = *req_id.as_bytes();\\n    // Mirror fiscal_documents columns exactly.\\n    sqlx::query(\\n        \\\"INSERT INTO\n<truncated 5079 bytes>",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\tests\\\\write_path_stage3_sign.rs\"",
  "toolAction": "\"Refactor seed_prepared_doc and make_worker_context in stage3_sign.rs\"",
  "toolSummary": "\"Edit test helpers\""
}
========================================

=== STEP: 328 ===
TOOL: multi_replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\tests\\write_path_stage5_finalize.rs"
ARGS: {
  "Description": "\"Refactor write_path_stage5_finalize.rs to use RequestId instead of [u8; 16] for test inbox row setup and assertions.\"",
  "Instruction": "\"Update write_path_stage5_finalize.rs to use RequestId.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":33,\"ReplacementContent\":\"use prro::db::models::enums::{DocState, NodeMode, ShiftState};\\nuse prro::db::models::ids::{DocumentId, RequestId};\",\"StartLine\":32,\"TargetContent\":\"use prro::db::models::enums::{DocState, NodeMode, ShiftState};\\nuse prro::db::models::ids::DocumentId;\"},{\"AllowMultiple\":false,\"EndLine\":164,\"ReplacementContent\":\"async fn seed_doc(\\n    pool: &SqlitePool,\\n    doc_byte: u8,\\n    lnd: i64,\\n    state: &str,\\n    unsigned_xml_sha256: Option<[u8; 32]>,\\n    previous_hash: Option<[u8; 32]>,\\n) -> (DocumentId, RequestId) {\\n    let doc_bytes = vec![doc_byte; 16];\\n    let req_bytes = vec![doc_byte ^ 0xFF; 16];\\n    let req_array: [u8; 16] = req_bytes.clone().try_into().unwrap();\\n    let req_id = RequestId::from_bytes(req_array);\\n    let payload_sha = vec![doc_byte; 32];\\n\\n    // Inbox row in PROCESSING (would be set by W5 acquire_lease).\\n    sqlx::query(\\n        \\\"INSERT INTO ingress_inbox(request_id, fiscal_number, protocol, operation_type, \\\\\\n            idempotency_key, payload_json, payload_sha256_canonical, status) \\\\\\n         VALUES (?, ?, 'REST', 'sell', ?, '{}', ?, 'PROCESSING')\\\",\\n    )\\n    .bind(&req_id)\\n    .bind(FN_ID)\\n    .bind(format!(\\\"idem-{doc_byte:02x}\\\"))\\n    .bind(&payload_sha)\\n    .execute(pool)\\n    .await\\n    .expect(\\\"seed inbox\\\");\\n\\n    // Doc row in target state.\\n    sqlx::query(\\n        \\\"INSERT INTO fiscal_documents(document_id, request_id, fiscal_number, lnd, doc_type, \\\\\\n            state, backend_profile_id, transport_profile_id, fs_mode, business_ts, payload_json, \\\\\\n            payload_sha256_canonical, unsigned_xml_sha256, previous_hash) \\\\\\n         VALUES (?, ?, ?, ?, 'SELL', ?, 'b1', 't1', 'ONLINE', \\\\\\n            '2026-05-09T12:34:56Z', '{}', ?, ?, ?)\\\",\\n    )\\n    .bind(&doc_bytes)\\n    .bind(&req_id)\\n    .bind(FN_ID)\\n    .bind(lnd)\\n    .bind(state)\\n    .bind(&payload_sha)\\n    .bind(unsigned_xml_sha256.as_ref().map(|h| &h[..]))\\n    .bind(previous_hash.as_ref().map(|h| &h[..]))\\n    .execute(pool)\n<truncated 5834 bytes>",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\tests\\\\write_path_stage5_finalize.rs\"",
  "toolAction": "\"Refactor write_path_stage5_finalize.rs to use RequestId\"",
  "toolSummary": "\"Edit test helpers\""
}
========================================

=== STEP: 351 ===
TOOL: replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\src\\services\\write_path\\stage_finalize.rs"
ARGS: {
  "AllowMultiple": "false",
  "Description": "\"Fix compilation error and compiler ICE in stage_finalize.rs by using inputs.request_id.as_bytes() instead of &inputs.request_id.\"",
  "EndLine": "341",
  "Instruction": "\"Use inputs.request_id.as_bytes() instead of &inputs.request_id when calling hex_encode.\"",
  "ReplacementContent": "\"            let payload = serde_json::json!({\\n                \\\"request_id\\\": hex_encode(inputs.request_id.as_bytes()),\\n                \\\"document_id\\\": hex_encode(doc.as_bytes()),\\n                \\\"fiscal_number\\\": inputs.fiscal_number,\\n                \\\"lnd\\\": inputs.lnd,\\n                \\\"unsigned_xml_sha256_hex\\\": hex_encode(&seed),\\n                \\\"previous_hash_hex\\\": inputs.previous_hash.map(|h| hex_encode(&h)),\\n            })\\n            .to_string();\"",
  "StartLine": "333",
  "TargetContent": "\"            let payload = serde_json::json!({\\n                \\\"request_id\\\": hex_encode(&inputs.request_id),\\n                \\\"document_id\\\": hex_encode(doc.as_bytes()),\\n                \\\"fiscal_number\\\": inputs.fiscal_number,\\n                \\\"lnd\\\": inputs.lnd,\\n                \\\"unsigned_xml_sha256_hex\\\": hex_encode(&seed),\\n                \\\"previous_hash_hex\\\": inputs.previous_hash.map(|h| hex_encode(&h)),\\n            })\\n            .to_string();\"",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\src\\\\services\\\\write_path\\\\stage_finalize.rs\"",
  "toolAction": "\"Fix hex_encode call with RequestId in stage_finalize.rs\"",
  "toolSummary": "\"Fix compile error\""
}
========================================

=== STEP: 455 ===
TOOL: multi_replace_file_content
FILE: "d:\\PRRO_GATE\\rust\\prro\\tests\\write_path_stage1_acquire.rs"
ARGS: {
  "Description": "\"Refactor write_path_stage1_acquire.rs to bind RequestId directly rather than converting to raw slice, resolving E0308 type mismatches.\"",
  "Instruction": "\"Bind RequestId directly instead of &req_id as a slice.\"",
  "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":281,\"ReplacementContent\":\"    sqlx::query(\\\"UPDATE ingress_inbox SET status = 'PROCESSING' WHERE request_id = ?\\\")\\n        .bind(&req_id)\",\"StartLine\":279,\"TargetContent\":\"    let req_slice: &[u8] = &req_id;\\n    sqlx::query(\\\"UPDATE ingress_inbox SET status = 'PROCESSING' WHERE request_id = ?\\\")\\n        .bind(req_slice)\"},{\"AllowMultiple\":false,\"EndLine\":338,\"ReplacementContent\":\"    sqlx::query(\\\"UPDATE ingress_inbox SET status = 'NEW' WHERE request_id = ?\\\")\\n        .bind(&req_id)\",\"StartLine\":336,\"TargetContent\":\"    let req_slice: &[u8] = &req_id;\\n    sqlx::query(\\\"UPDATE ingress_inbox SET status = 'NEW' WHERE request_id = ?\\\")\\n        .bind(req_slice)\"},{\"AllowMultiple\":false,\"EndLine\":755,\"ReplacementContent\":\"    let doc_id = prro::db::models::ids::DocumentId::new();\\n    sqlx::query(\\n        \\\"INSERT INTO fiscal_documents (\\n             document_id, request_id, fiscal_number, lnd, doc_type, state,\\n             backend_profile_id, transport_profile_id, fs_mode, business_ts,\\n             total_sum_kop, payload_json, payload_sha256_canonical\\n         ) VALUES (?, ?, ?, 999, 'SELL', 'ACK', 'b', 't', 'ONLINE', ?, ?, ?, ?)\\\",\\n    )\\n    .bind(doc_id)\\n    .bind(&req_id)\",\"StartLine\":745,\"TargetContent\":\"    let req_slice: &[u8] = &req_id;\\n    let doc_id = prro::db::models::ids::DocumentId::new();\\n    sqlx::query(\\n        \\\"INSERT INTO fiscal_documents (\\n             document_id, request_id, fiscal_number, lnd, doc_type, state,\\n             backend_profile_id, transport_profile_id, fs_mode, business_ts,\\n             total_sum_kop, payload_json, payload_sha256_canonical\\n         ) VALUES (?, ?, ?, 999, 'SELL', 'ACK', 'b', 't', 'ONLINE', ?, ?, ?, ?)\\\",\\n    )\\n    .bind(doc_id)\\n    .bind(req_slice)\"}]",
  "TargetFile": "\"d:\\\\PRRO_GATE\\\\rust\\\\prro\\\\tests\\\\write_path_stage1_acquire.rs\"",
  "toolAction": "\"Fix mismatched types in write_path_stage1_acquire.rs by binding RequestId directly\"",
  "toolSummary": "\"Fix compile errors\""
}
========================================

