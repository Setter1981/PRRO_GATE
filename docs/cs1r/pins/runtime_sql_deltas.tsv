# CS-1R3 A2 + A4 — catalogued RUNTIME-SQL deltas (DATA, code-owner-gated).
#
# Each row is an APPROVED (base_sql, head_sql) pair: CS-1 changed the RUNTIME
# `sqlx::query*` statement text (removed a `col as "alias: Type"` decode annotation,
# moving the type to a turbofish). For the RUNTIME sqlx API the whole string is sent
# VERBATIM to SQLite (confirmed via sqlx-macros-core 0.8.6 src/query/{input,output}.rs
# + the .sqlx describe cache — see docs/cs1r/PRODUCTION_SQL_DELTAS.md), so removing
# the alias DOES change the executed SQL bytes. The fiscal RESULT + persisted
# representation are unchanged (the alias only named a read's output column). These —
# and ONLY these — runtime-SQL edits are accepted by the provenance sqlx-signature
# check; any other SQL change is RED.
#
# This lives in a DATA file (not next to the oracle) so a re-pin is a visible,
# CODEOWNER-gated data diff. NOTE: this catalog covers the TEST-side runtime deltas
# the provenance tool audits (the 79 test files). The PRODUCTION-side src deltas are
# catalogued separately in docs/cs1r/PRODUCTION_SQL_DELTAS.md (out of the test tool's
# scope).
#
# Format (tab-separated, one row per delta; `#` comments + blank lines ignored):
#   <base_sql>	<head_sql>
SELECT state as "state: ShiftState" FROM shifts WHERE shift_id = ?	SELECT state FROM shifts WHERE shift_id = ?
SELECT shift_state as "s: ShiftState" FROM node_state WHERE fiscal_number = ?	SELECT shift_state FROM node_state WHERE fiscal_number = ?
